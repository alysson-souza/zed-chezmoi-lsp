[package]
name = "zed-chezmoi-lsp"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Chezmoi template support with configurable host language LSP proxying for Zed"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"
